<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Report</title>
</head>
<body>
    <h1>Test Report</h1>
    <ul>
        {% for item in data %}
        <li>Line: {{ item.line }}
            <ul>
                {% for element in item.elements %}
                <li>
                    Element Line: {{ element.line }}
                    {% if element.name %}
                    Name: {{ element.name }}
                    {% endif %}
                    {% if element.before %}
                    <ul>
                        {% for before_item in element.before %}
                        <li>{{ before_item }}</li>
                        {% endfor %}
                    </ul>
                    {% endif %}
                    {% if element.after %}
                    <ul>
                        {% for after_item in element.after %}
                        <li>{{ after_item }}</li>
                        {% endfor %}
                    </ul>
                    {% endif %}
                </li>
                {% endfor %}
            </ul>
        </li>
        {% endfor %}
    </ul>
</body>
</html>
