<!DOCTYPE html>
<html>
<head>
    <title>Cucumber Test Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 40px;
            background-color: #f7f9fc;
        }
        h2 {
            color: #333;
            border-bottom: 2px solid #e0e0e0;
            padding-bottom: 10px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        th, td {
            border: 1px solid #e0e0e0;
            padding: 10px 15px;
            text-align: left;
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
        tr:hover {
            background-color: #f5f5f5;
        }
        td {
            color: #555;
        }
        .status-passed {
            color: green;
        }
        .status-failed {
            color: red;
        }
    </style>
</head>
<body>

<h2>Cucumber Test Report</h2>

<table>
    <tr>
        <th>Feature</th>
        <th>Scenario</th>
        <th>Status</th>
    </tr>
    {% for feature in data %}
        <tr>
            <td rowspan="{{ feature.elements|length }}">{{ feature.name }}</td>
            {% for scenario in feature.elements %}
                <td>{{ scenario.name }}</td>
                <td class="status-{{ scenario.status }}">{{ scenario.status|capitalize }}</td>
            </tr>
            {% endfor %}
    {% endfor %}
</table>

</body>
</html>
